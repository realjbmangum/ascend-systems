## Session - May 12, 2026

**Completed (P0 security):**
- Deleted `POST /api/auth/test-login` backdoor from `worker/src/routes/auth.ts` (CRM #92).
  Endpoint took any `{email, role}` body and minted a 30-day session cookie — no
  password, no magic-link, no verification. Anyone hitting the URL with `role:"admin"`
  got full CRM access. No callers anywhere in the codebase (grep clean). Deployed.
- Verified: `POST /api/auth/test-login` now returns `401 unauthorized` (falls through
  to the admin sub-app's auth gate at line 739 of `src/index.ts`). Real magic-link
  flow unaffected.

**Completed (ICP + strategy docs):**
- ICP locked: **Charlotte-metro SMBs, geographic — not vertical-specific.**
  $1M–$50M revenue, owner-operator decision-maker, growing fast. Industries
  rank-ordered: property mgmt, professional services, multi-location services,
  trades $5M+, healthcare, light mfg, DTC. Closes CRM #95.
- Wrote three master strategy docs in `docs/` (HTML format per Brian's
  override of the global .md rule — they need to be browser-viewable, sendable
  as web pages):
    - `docs/marketing-strategy.html` (~4,200 words) — 5-channel ranked plan
      (warm 30 / in-person 25 / content+tools 25 / cold 15 / paid 5), ICP rank
      table, value prop vs 5 alternatives, 30/60/90 plan, kill criteria
    - `docs/outreach-playbook.html` — Outscraper filter recipe, warm
      reactivation channel (Week 1 burn-down — 100 personal touches before
      any cold), three full 5-email sequences (Generic-local / Trigger-based
      / Mutual-connection), LinkedIn templates, 20-min discovery call script
    - `docs/charlotte-target-list.html` — 62 real Charlotte-metro companies
      sourced from CBJ Fast 50, Inc 5000, Expertise.com, BCG legal rankings.
      27 P1 / 33 P2 / 14 P3. Top P1: Opkalla (Aaron Bock), Hylaine (CBJ Fast
      50 #22), Tonidandel-Brown Restaurant Group.
- `docs/_doc-styles.html` shared brand-styled `<style>` block all three docs
  embed verbatim (Inter + JetBrains Mono, charcoal/orange brand palette).
- Three docs generated by three parallel background agents using the
  `business/marketing-plan`, `marketing/copywriting+email-sequence`, and
  `business/lead-research` skills respectively.
- Doc revisions after Brian's review: added Outscraper filter recipe to
  outreach-playbook §2, added Warm Reactivation as channel 3a in
  outreach-playbook §3 (renumbered in-person→3b, cold email→3c, LinkedIn→3d),
  added Week 1 burn-down callout in cadence, reframed cold reply rates honestly
  at 1–3%. In marketing-strategy: rewrote channel rank to 5-row time-budget
  table (warm/in-person/content+tools/cold/paid), added new Channel 1 Warm
  Reactivation subsection with the exact message template, replaced
  Channel 3 with Content + Free Tools (lists Cost Calculator + Scorecard +
  Build-vs-Buy + Charlotte SMB Tech Report), reframed Channel 5 Paid as
  "amplification not cold acquisition" with $300/mo cap.

**Completed (Cost Calculator MVP — first free tool shipped):**
- New worker endpoint `POST /api/tools/cost-calculator` at
  `worker/src/routes/tools.ts`. 4 inputs (hours/week, hourly cost, error rate,
  customer impact "none"|"slows"|"loses"). Banded recommendation: DIY (<$10k
  waste) → light automation ($10k–50k waste, $5–15k build, 2–4 mo payback) →
  custom build ($50k–150k waste, $15–40k build) → strategic platform (>$150k,
  $40k+ build). Inserts lead + task in `leads`+`tasks`, enrolls in
  `calculator_followup` drip, sends prospect report email + admin alert.
- `sendCalculatorReport()` added to `worker/src/email.ts` — branded HTML
  report email. v1 had no logo; v2 (after Brian feedback) ships a proper
  light-bg logo header (charcoal wordmark + orange mark per brand-kit "light
  bg" rule, 200px wide pulled from
  https://ascendsystems.ai/images/logo.png), email-safe table-only layout
  (Gmail/Outlook compatible), system font stack, charcoal CTA card, footer
  with brand wordmark + site link, Brian's personal sign-off.
- React page at `src/pages/tools/CostCalculator.tsx` — form with 4 calculator
  inputs + name/email/optional company + optional process description.
  On submit shows result inline (big-number hero, breakdown table,
  recommendation card, build/payback range, CTA to /contact) AND emails
  the branded report.
- Route added at `/tools/cost-calculator` in `src/App.tsx`.
- Site Tailwind theme uses cooler tones (#1E2A32 charcoal, #D4632C orange)
  vs the warmer brand-kit (#1C1C1E, #C45A2C); page uses the site palette,
  email uses the live-site palette (#1E2A32/#D4632C) to match what users
  see in the live header.
- Smoke-tested live: Lead #22 created at $64k/yr / custom band, Lead #23
  at $86k/yr from the branded v2 email retest. Both sent to bmangum1@gmail.com.

**Files Changed:**
- worker/src/routes/auth.ts — deleted /test-login handler (39 lines)
- worker/src/routes/tools.ts — NEW: calculator endpoint + compute logic
- worker/src/email.ts — NEW: sendCalculatorReport (logo + table layout)
- worker/src/index.ts — mounted /api/tools router
- src/pages/tools/CostCalculator.tsx — NEW: form + result page
- src/App.tsx — added /tools/cost-calculator route
- src/lib/api.ts — added submitCostCalculator typed call
- docs/_doc-styles.html — NEW: shared brand style block for HTML strategy docs
- docs/marketing-strategy.html — NEW: master GTM doc (revised with warm-first)
- docs/outreach-playbook.html — NEW: executable companion (revised with Outscraper + warm)
- docs/charlotte-target-list.html — NEW: 62-company sourced target list

**Decisions:**
- ICP: Charlotte-metro geographic, $1M–$50M SMBs, owner-operator
  decision-maker. Not vertical-specific.
- Strategy docs in `site-ai-tech-co/` use HTML (not Markdown) — overrides
  global .md rule for this project only. Memory saved.
- Channel mix rebalanced to warm-first (30%) vs cold-heavy original draft.
  Cold reply rate framed honestly at 1–3% (not the over-optimistic 5–15%
  in the agent's first pass).
- First free tool = Manual Process Cost Calculator. Three more queued
  in marketing-strategy.html (AI Readiness Scorecard, CRM Build-vs-Buy,
  Charlotte SMB Tech Report).
- Calculator email uses the LIVE-SITE palette (#1E2A32/#D4632C) not the
  brand-kit warmer tones, so the report email matches what visitors see
  on ascendsystems.ai.

**Gotchas + portfolio-wide learnings:**
- Brand-kit.md (#1C1C1E charcoal, #C45A2C orange) and the live site theme
  in `src/styles/globals.css` (#1E2A32 charcoal, #D4632C orange) DIFFER.
  When making new branded artifacts, anchor to the live site theme not
  the brand-kit doc.
- Google Font inline-loading (`<link rel="stylesheet" href="fonts.googleapis...">`)
  in email is unreliable — Gmail strips, Outlook ignores. Use system font
  stacks (`-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial`)
  for email instead.
- Email layouts need table-based structure — `flex`/`grid` don't survive
  Gmail's render pass. The v1 email used flex-style div nesting; v2 was
  rewritten as nested `<table>` elements.

**Next Steps:**
- [ ] Drag CRM #92 + #95 to Done in kanban
- [ ] Brian: review live `ascendsystems.ai/tools/cost-calculator` after CF Pages
      deploys (1–2 min after push of 5ba43c0)
- [ ] Confirm v2 branded email lands cleanly (logo loads, no broken layout in Gmail/Apple Mail)
- [ ] Begin warm reactivation (Week 1 of marketing strategy): 100 personal
      touches to past clients, friends, dormant LinkedIn connections
- [ ] Seed `calculator_followup` drip sequence in email_sequences table
      (currently enrollByTrigger silently fails — needs a sequence row)
- [ ] Build the next free tool: AI Readiness Scorecard (10 yes/no Qs)
- [ ] Update Services page copy / pricing to anchor on Charlotte ICP
      ($2.5k Discovery → $10–50k Build → $2–8k/mo retainer per marketing-strategy.html)

---

## Session - May 3, 2026

**Completed:**
- Full backend build: Hono worker with D1 database
- Authentication: Magic-link sign-in for admin and client roles
- Task queue system: Triggered by form submissions and Stripe webhooks
- Stripe invoicing: Full integration with line items, payment tracking, webhooks
- Email drips: Cloudflare Email Workers with sequence management and cron processor
- Project notes: Permission-based visibility (visible_to_client flag)
- Admin UI: Tasks, Invoices, InvoiceDetail, EmailSequences pages, ProjectDetail notes tab
- Client portal: Login, Projects, Invoices pages with read-only access
- Contact and intake forms: Wired to create tasks and enroll in email sequences
- Worker deployment: ascend-api.bmangum1.workers.dev live
- Schema migration: 9 new tables (12 total with existing leads, clients, projects)
- Pages deployment: Redeployed with all new admin + portal routes
- Integration testing: All public endpoints verified and working

**Decisions:**
- Used Cloudflare Email Workers (MailChannels) instead of SendGrid for email
- Magic links expire in 15 minutes (stored in D1 with token, role, expires_at)
- Tasks have optional client_id and lead_id for context
- Email sequences use "trigger" field for enrollment grouping (e.g., "lead_welcome")
- 15-minute cron for drip processor instead of on-demand to reduce API calls
- Session cookies are HttpOnly + SameSite=Strict for security
- Project notes visibility is boolean flag, not role-based (simpler for now)

**Gotchas:**
- Stripe utility functions need nodejs_compat flag in wrangler.toml
- D1 schema can't use CREATE TABLE (would fail on existing tables) — migration used CREATE TABLE IF NOT EXISTS
- MailChannels requires SPF/DKIM DNS records on noreply@ascendsystems.ai domain
- Stripe webhook requires raw body for signature verification — use c.req.raw.text() not c.req.json()
- Magic link tokens must be URL-encoded when passed as query params
- Session tokens must be validated against expires_at datetime('now') comparison

---

## Session - May 4, 2026

**Completed:**
- Switched email from MailChannels to SendGrid (MailChannels kept returning 401 — DNS was correct but never resolved)
- Fixed SameSite cookie bug: changed from Strict to None to allow cross-origin cookies (frontend ascendsystems.ai ↔ API ascend-api.bmangum1.workers.dev)
- Fixed magic link verify flow: created Verify.tsx + added /admin/auth/verify route
- Cleaned D1 database: removed test leads/clients, kept Chris Rutherford
- Cloudflare Zero Trust: set up Access policy on ascendsystems.ai/admin/* with one-time PIN (email: bmangum1@gmail.com)
- Dashboard rebuild: 4 stat cards, pipeline overview, recent leads + open tasks
- Delete buttons on all list pages (leads, clients, projects, invoices) with cascade handling
- Task creation: modal on Tasks page + inline form on ProjectDetail
- Task-project association: tasks now have project_id FK, project detail shows linked tasks
- Sort/filter dropdowns on all list pages (Leads, Clients, Projects, Invoices)
- Kanban board for Projects: list/kanban toggle, 6 status columns, color-coded
- Email templates: all 3 templates (magic link, form confirmation, admin alert) use branded dark header + orange accent
- R2 file uploads on Projects: Files tab, click-to-upload, download (auth-gated proxy), delete
- Stripe one-time invoice push: "Send via Stripe" button, auto-creates Stripe customer, stores payment URL
- Stripe recurring billing: billing_type toggle on invoice creation, "Set up Recurring" creates Stripe Subscription
- Stripe bug fixes: replaced node:crypto with Web Crypto API, fixed customerId.id type bug
- Subscriptions table + page: track active Stripe subscriptions, cancel from admin
- Proposals: full CRUD, sign token generation, public sign page at /proposals/:token
- D1 migrations: added stripe_customer_id to clients, billing_type/recurring_interval/stripe_payment_url/stripe_subscription_id to invoices, start_date to projects, created proposals + subscriptions tables

**Files Changed:**
- worker/src/stripe.ts - Replaced node:crypto with Web Crypto API, added createSubscription + cancelSubscription
- worker/src/routes/invoices.ts - Fixed customerId bug, added billing_type/recurring_interval to INSERT, added push-recurring endpoint, stores stripe_customer_id on client
- worker/src/routes/proposals.ts - NEW: full proposals CRUD + send endpoint
- worker/src/index.ts - Added R2 file routes, proposal public sign routes, subscriptions CRUD, proposal routes mounted
- worker/src/types.ts - Added FILES_BUCKET: R2Bucket binding
- worker/src/email.ts - Full SendGrid rewrite, branded HTML templates
- worker/wrangler.toml - Added R2 bucket binding (ascend-files)
- worker/src/routes/auth.ts - Changed SameSite from Strict to None
- src/pages/admin/Projects.tsx - List/Kanban toggle
- src/pages/admin/ProjectsKanban.tsx - NEW: Kanban board component
- src/pages/admin/ProjectDetail.tsx - Files tab, tasks inline form
- src/pages/admin/Tasks.tsx - Create modal, delete button
- src/pages/admin/InvoiceDetail.tsx - Send via Stripe button, recurring button, payment URL display
- src/pages/admin/Invoices.tsx - billing_type toggle, sort/filter
- src/pages/admin/CreateInvoice.tsx - billing_type + recurring_interval fields
- src/pages/admin/Proposals.tsx - NEW: proposals list page
- src/pages/admin/CreateProposal.tsx - NEW: proposal create form
- src/pages/admin/ProposalDetail.tsx - NEW: proposal detail with send + sign URL
- src/pages/admin/Subscriptions.tsx - NEW: subscriptions list with cancel
- src/pages/admin/Dashboard.tsx - Rebuilt with stat cards, pipeline, recent activity
- src/pages/ProposalSign.tsx - NEW: public client-facing sign page
- src/App.tsx - Added proposal + subscription routes
- src/components/AdminLayout.tsx - Added Proposals + Subscriptions nav links
- src/lib/api.ts - Added file, proposal, subscription, recurring invoice methods

**Decisions:**
- Ascend is source of truth; Stripe is payment layer only — invoices draft in D1, push to Stripe on send
- Recurring billing uses Stripe Subscriptions with inline prices (no product catalog needed)
- Proposals use a unique sign_token (UUID) — no auth required to view/sign, just the link
- R2 file downloads proxied through worker (auth-gated) rather than presigned URLs
- STRIPE_WEBHOOK_SECRET added to Cloudflare via dashboard GUI

**Infrastructure:**
- R2 bucket: ascend-files (created this session)
- Stripe webhook: https://ascend-api.bmangum1.workers.dev/api/webhooks/stripe
  - Events: invoice.paid, invoice.payment_failed, invoice.marked_uncollectible, customer.subscription.deleted
- Secrets in Cloudflare worker: SENDGRID_API_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET

**Next Steps:**
- [ ] Test Stripe invoice send end-to-end with a real client
- [ ] Test recurring billing setup + first payment
- [ ] Test proposal sign flow (create → send link → client signs)
- [ ] Set up client portal magic link for first real client
- [ ] Seed welcome email sequence (lead_welcome trigger)
- [ ] Consider adding PDF export for proposals
- [ ] Consider adding invoice PDF download

---

## Session - July 18, 2026 — SEO: quick fixes + Set 1 programmatic pages

**Context:** Site had four SEO strategy docs from May 12, 2026 (docs/seo/) with
the technical foundation shipped but the content/programmatic backlog untouched.
Audited docs vs. implementation, fixed the broken sitewide assets, then built
the first programmatic page set.

**Completed — sitewide fixes:**
- Generated public/og-image.png, favicon.ico, apple-touch-icon.png. BOTH the OG
  image and favicon were referenced by Layout.astro on every page but did not
  exist — every social share was a broken image. Reproducible via
  `python3 scripts/gen-brand-assets.py`.
- Wired the hand-written seoTitle/seoDescription from case-study frontmatter
  into portfolio/[slug].astro. They had been authored months ago and never
  consumed — all 8 case studies were shipping 33–64 char meta descriptions.
- Enriched Article schema: image, datePublished, dateModified, mainEntityOfPage,
  isPartOf, keywords; `about` now uses Thing objects.
- Added publishDate/updatedDate to the case-studies collection, backfilled from
  git history.
- Trimmed Home (76→53) and Services (63→59) titles under the 60-char SERP limit.
- Added ProfilePage schema to /about and ContactPage schema to /contact, plus
  BreadcrumbList on both. Both reference sitewide @ids rather than duplicating.
- Added public/llms.txt for AI crawler/citation visibility.
- Added apple-touch-icon link + sizes="any" on favicon in Layout.astro.
- Nav isActive() now prefix-matches so child routes highlight their parent.

**Completed — Set 1 programmatic pages (5 live):**
- src/data/services.ts — SERVICE_PAGES, 5 entries, ~1,450 words each
- src/pages/services/[slug].astro — template, plain Astro (not PageIsland)
- Hub links section added to /services + Services column added to AstroFooter
- Verified against the plan's pre-publish checklist: all 5 pages pass on word
  count (1,441–1,501 vs 1,000 floor), title ≤60, description 150–160, 6 FAQs
  each at 80+ words, ≥3 inbound/outbound internal links, static HTML, sitemap.
- Cross-page sentence overlap 1.8% (plan target: <40%).
- 22 pages build clean; 41+ JSON-LD blocks validate, 0 invalid.

**Decisions:**
- SERVICE SLUGS follow the five service lines actually sold (custom-saas-development,
  internal-tools, legacy-modernization, ai-integrations, fractional-cto), NOT the
  doc's proposed custom-crm / web-application-development / process-automation.
  Only fractional-cto overlapped; the rest described services not on the site.
- PRICING on the new pages uses the real site numbers ($5k discovery sprint,
  $15k–$50k+ builds) from index.astro FAQ. The May doc's "$3,500 / $15K–$75K"
  figures do NOT match what's actually sold — do not copy pricing from that doc.
- Article datePublished uses real publish dates from git, NOT engagementStart.
  Using engagement dates would have claimed the PottyDirectory article was
  published in 2024.
- OG image falls back to site default unless the hero file exists on disk
  (build-time existsSync check) — several `hero:` frontmatter paths point at
  images that were never committed.
- CHARLOTTE PAGE SET (Set 2) DEFERRED — see blockers below.

**Blockers on the Charlotte /charlotte/[service-slug] set (5 pages, highest
value in the plan — these target the top-priority local keywords):**
- [ ] No phone number. siteConfig.phone is '' — blocks the required NAP block
      and LocalBusiness schema completeness. Brian has a number, needs to supply it.
- [ ] Stancil Services is NOT a client. The plan builds these pages around
      "City of Charlotte + Stancil Services" as named local proof, but Stancil
      appears only in docs/outreach-playbook.html and charlotte-target-list.html
      — it's a prospect. Only clt-ev is real local proof.
- [ ] /charlotte/process-automation has no target keyword in the plan.
- [ ] Pages need local pricing claims and fair competitor characterizations
      (Designli, Bry AI, Dualboot) that need Brian's input, not invention.

**Other outstanding SEO items:**
- [ ] 34 case-study screenshots referenced in markdown bodies do not exist —
      public/images/case-studies/ is missing entirely. These render as broken
      images on live case study pages. Highest-priority content gap.
- [ ] /portfolio/heirloom is a thin page — no .md file, builds with Article
      schema but no body. Either write it or noindex it.
- [ ] No AI-integration case study exists. /services/ai-integrations says so
      honestly rather than implying one; replace that section when real AI work ships.
- [ ] Google Search Console + Bing Webmaster Tools NOT set up. Bing matters —
      it feeds ChatGPT and Perplexity.
- [ ] Several case-study seoTitles exceed 60 chars (Deadrop is 78) — they now
      render as-is rather than being silently rewritten.
- [ ] Blog / content strategy (12 pieces) and remaining programmatic sets
      (/tools, /vs, /industries) still unbuilt.

**Gotchas:**
- public/brand-v2/logo-mark-only.png is NOT mark-only — it has an "ASCEND
  SYSTEMS" wordmark band below the glyph (~93% height). The asset script crops it.
- The mark's cross detail is opaque WHITE on navy, so a naive navy→white recolor
  erases it. gen-brand-assets.py recolors three ways and drops the cross at
  favicon sizes where it's just noise.
- Astro preview + the Chrome extension would not render the local site (extension
  reported an error page on a URL curl served 200). Visual design review of the
  new service pages is still OUTSTANDING — verified structurally only.

**Code review (medium) — 6 findings, 2 fixed, 4 open:**
- FIXED: the /services hub links were rendering BELOW the React island's
  closing CTA ("Ready to get started?"), stranding them past the page's
  conversion moment. Moved into src/pages/Services.tsx above its final CTA
  section — still SSR'd into static HTML, so no SEO loss. services.astro no
  longer appends its own section.
- FIXED: AstroFooter brand column had md:col-span-2 but no sm:col-span-2 after
  the grid gained an sm:grid-cols-2 breakpoint — brand block would squeeze
  into a quarter-width column at tablet.
- OPEN (needs Brian's call): THREE-WAY SERVICE NAME MISMATCH. The visible
  /services page lists 4 services (Web & App Development, AI Integrations,
  Business Automation, Custom Solutions — Services.tsx:9-57) while the Service
  schema and the new detail pages use a different set of 5 (Custom SaaS
  Development, Internal Tools & Dashboards, Legacy System Modernization,
  AI Integrations & Agents, Fractional CTO). This predates the SEO work — the
  schema and the UI already disagreed — but the new pages + footer now surface
  the mismatch to visitors. Pick one canonical set of service names.
- OPEN: site origin 'https://ascendsystems.ai' hardcoded in 62 places across
  src/. astro.config.mjs already sets `site` and siteConfig has `domain`.
  Should use Astro.site / a shared constant.
- OPEN: the build-time existsSync OG guard covers `hero` only. `logo` and
  `screenshot` in portfolio.ts get no check, and heirloom-logo.png is already
  deleted-but-referenced (portfolio.ts:194) so it renders broken.
- OPEN: Article `image` falls back to the generic og-image.png on 7 of 8 case
  studies because the hero files don't exist. Self-corrects once they land.

---

## Session - July 18, 2026 (continued) — phone, service alignment, image plumbing

**Completed:**
- Published the real phone: (980) 577-1231 in siteConfig (renders in AstroFooter,
  Footer.tsx, and as a tel: link on /contact — all three were already
  conditionally wired, just waiting on a value) and +1-980-577-1231 as
  `telephone` on ProfessionalService. LocalBusiness schema is now complete.
- Rewrote src/pages/Services.tsx from 4 services to the canonical 5. The
  homepage (ServicesSection.tsx) and the Service schema BOTH already had the
  correct 5 — only /services was stale. Each card now deep-links to its
  /services/[slug] detail page.
- Removed a false claim: the old AI entry said "We built an AI phone system
  that answers calls around the clock... doing the job of a $3,000/month
  answering service." No such project exists in the portfolio. Replaced with an
  approach description, and the callout label is now data-driven
  (`exampleLabel`) so it reads "How Engagements Start" instead of "Real
  Example". Restore the default label when a real AI engagement ships.
- Wired image rendering that never existed: the `screenshots` frontmatter was
  declared in content.config.ts but NO template consumed it. Added a gallery to
  portfolio/[slug].astro and an illustration slot to services/[slug].astro,
  both guarded by build-time existsSync so missing files are skipped.
- docs/image-generation-prompts.md — 39 prompts (5 service + 34 case study)
  with exact target filenames, a shared house-style block, and a verification
  script.

**Correction to the earlier entry in this file:** I previously wrote that the
34 missing case-study screenshots "render as broken images on live case study
pages." That was WRONG. They were declared in frontmatter and never rendered at
all. They render now (behind the existsSync guard) because I wired the gallery.

**SECURITY — repo hygiene:**
- github.com/realjbmangum/ascend-systems is PUBLIC. Found untracked in the repo
  root: Invoice-EULMKGF7-0001.pdf, PO2610021167....pdf, four
  A-Assumed-Business-Name-Certificate PDFs, and ascend-dba-filing-guide.html.
  None were caught by .gitignore — a single `git add -A` would have published
  business financial and registration records.
- .gitignore now excludes *.pdf, Invoice-*, PO[0-9]*, the certificates, and
  Ascend V2/. Verified via `git log --all` that none were EVER committed, so no
  history rewrite is needed.
- Worth auditing the other public repos for the same pattern.

**Decisions:**
- Committed on branch `feat/seo-service-pages` rather than straight to main,
  and pushed. Not merged — Brian's call.
- `IMAGES_ARE_ILLUSTRATIVE = true` in portfolio/[slug].astro adds an
  "illustrative mockup" caption to case-study screenshots. Flip to false once
  real captures replace the generated set. 4 of the 5 products (scdmvappointments,
  recordstops, pottydirectory, sendmylove) are LIVE and can just be captured —
  that is the better path than generating.
- No generated screenshots for CLT EV (real City of Charlotte client work, real
  capture already exists), Ringdocket, or Heirloom.

**Next steps:**
- [ ] Merge feat/seo-service-pages (PR or direct)
- [ ] Grok Voice Agent: two agents, SEPARATE knowledge bases. Caller-facing gets
      only services.ts, case studies, website-copy.md, MSA. It must NOT get
      docs/charlotte-target-list.html, docs/outscraper/*, outreach-playbook,
      warm-reactivation-emails, the security review, or competitive analysis.
- [ ] Drop generated images per docs/image-generation-prompts.md
- [ ] Google Search Console + Bing Webmaster Tools (Bing feeds ChatGPT/Perplexity)
- [ ] Charlotte page set still deferred — now unblocked on phone, still blocked
      on a real local case study (Stancil is a prospect, not a client)
- [ ] Visual design review of the 5 new service pages still OUTSTANDING

## Session - July 18, 2026 (final) — screenshot cleanup, service images, PR #1

**THE REAL SCREENSHOT PROBLEM (found late):** the case study markdown bodies
contained 39 literal `{{screenshot: name}}` placeholder tokens that rendered as
VISIBLE TEXT on the live pages — "{{screenshot: homepage}}" was showing
mid-article on /portfolio/recordstops and five others. Removed all 39 (both
standalone lines and inline at paragraph ends). This, not the missing image
files, was the actual visible defect.

**Removed the case-study image machinery entirely** per Brian's call ("we don't
need that many screenshots, the text is fine"):
- `screenshots` + `hero` dropped from content.config.ts and all 7 case studies
- the gallery added earlier in this branch removed, with IMAGES_ARE_ILLUSTRATIVE
- case studies now use the site OG image; write-ups stand on text alone
- docs/image-generation-prompts.md trimmed from 39 prompts to the 5 service ones

**Service images in place (5/5):**
- Brian generated them and saved to "public/Services Screens/" — moved to
  public/images/services/. The original folder name had a SPACE and capitals,
  which requires URL-encoding and breaks on case-sensitive hosting.
- Extensions corrected .png -> .jpg to match the actual files
- Each renders with an "illustrative mockup, not a client system" caption
- KNOWN ARTIFACT: internal-tools.jpg reads "Siter" instead of "Sites" in the
  first stat tile. Regenerate that one when convenient — prompt is in the doc.

**Shipped:** branch `feat/seo-service-pages`, 3 commits (d854411, ec1e29c,
ca786b5), **PR #1 open**: https://github.com/realjbmangum/ascend-systems/pull/1

**Final verification (all green):** 5 service pages at 1,463-1,523 words, titles
<=60, descriptions 150-160, 6 FAQs each, 21 inbound links each, 1.8% cross-page
overlap, 62 JSON-LD blocks 0 invalid, no {{screenshot}} tokens anywhere in dist/.

**Still open:**
- [ ] Visual design review of the 5 service pages — STILL OUTSTANDING, browser
      extension would not render the local preview. Worth eyeballing before merge.
- [ ] Regenerate internal-tools.jpg ("Siter" typo)
- [ ] Merge PR #1
- [ ] Grok Voice Agent — two agents, SEPARATE knowledge bases. Caller-facing must
      never get charlotte-target-list.html, docs/outscraper/*, outreach-playbook,
      warm-reactivation-emails, or the security review.
- [ ] GSC + Bing Webmaster Tools
- [ ] Audit other public repos for stray PDFs/invoices in the root
- [ ] Charlotte set: unblocked on phone, still needs a real local case study
- [ ] 62 hardcoded site origins in src/ — use Astro.site
